空を尋ねて「 」で区切る。
それを配列数値変換。
Nはそれ@0
Mはそれ@1

空を尋ねて「 」で区切る。
それを配列数値変換。
Xはそれ
Xを配列数値ソート。

B = []
iを0からN-2まで繰り返す。。
    b = X[i+1] - X[i]
    Bにbを追加。
ここまで。
Bを配列数値ソート。

答え = 0
もし(N == M)ならば、
    0を表示。
    終了。
ここまで。
iを0からN-M-1まで繰り返す。
    答え = 答え + B[i]
ここまで。

答えを表示。
終了。